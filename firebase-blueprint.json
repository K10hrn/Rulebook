{
  "entities": {
    "Rulebook": {
      "title": "Rulebook",
      "description": "Metadata for an uploaded board game rulebook",
      "type": "object",
      "properties": {
        "name": { "type": "string", "description": "The name of the board game" },
        "size": { "type": "number", "description": "The file size in bytes" },
        "date": { "type": "number", "description": "The upload timestamp" },
        "storagePath": { "type": "string", "description": "The path to the PDF in Firebase Storage" }
      },
      "required": ["name", "size", "date", "storagePath"]
    }
  },
  "firestore": {
    "/users/{userId}/rulebooks/{rulebookId}": {
      "schema": "Rulebook",
      "description": "User's personal library of board game rulebooks"
    }
  }
}
